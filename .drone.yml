kind: pipeline
name: plugin-tag

trigger:
  branch:
    - plugin

platform:
  os: linux
  arch: amd64

steps:
  - name: plugin-zip
    image: docker
    commands:
    - echo "test"
    - mkdir -p plugins/opni-dashboards-plugin
    - mv * plugins/opni-dashboards-plugin
    - ls
    - ls plugins/opni-dashboards-plugin
